# Badge & Chip — [APP] Salesman Mobile

> Class: `.app-badge`, `.app-chip`

---

## Status badge

| Variant | Class | Màu |
|---------|-------|-----|
| Success | `.app-badge--success` | Nền `--success-50`, chữ `--success-700` |
| Warning | `.app-badge--warning` | Nền `--warning-50`, chữ `--warning-700` |
| Error | `.app-badge--error` | Nền `--error-50`, chữ `--error-500` |
| Neutral | `.app-badge--neutral` | Nền `--neutral-100`, chữ `--neutral-700` |
| Primary | `.app-badge--primary` | Nền `--app-primary-light`, chữ `--app-primary-darker` |

Base: `.app-badge` — padding 2px 8px, radius `--radius-sm`, font 11px / 600

---

## Filter chip

| Class | State |
|-------|-------|
| `.app-chip` | Viền `--neutral-200`, nền trắng |
| `.app-chip--active` | Nền `--app-primary-light`, viền `--app-primary`, chữ primary |

Horizontal scroll row: `.app-chip-row`

---

## Nav badge (số thông báo)

`.nav-badge` — xem `navigation.md`

---

## Change History

| Phiên bản | Ngày | Mô tả |
|-----------|------|-------|
| 1.0 | 24/06/2026 | — |
